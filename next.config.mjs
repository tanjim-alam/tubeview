/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["i0.wp.com", "viplikes.net", "www.ytviews.in", "upload.wikimedia.org", "media.licdn.com"]
    }
};

export default nextConfig;
